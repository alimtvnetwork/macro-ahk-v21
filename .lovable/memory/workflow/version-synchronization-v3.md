# Memory: workflow/version-synchronization-v3
Updated: 2026-04-09

The extension version is 2.137.0, synchronized exactly across `chrome-extension/manifest.json` (`version` and `version_name`), `src/shared/constants.ts`, `src/options/sections/AboutSection.tsx`, `standalone-scripts/macro-controller/src/instruction.ts`, `standalone-scripts/marco-sdk/src/instruction.ts`, and `standalone-scripts/xpath/src/instruction.ts`. Version consistency is validated via `scripts/check-version-sync.mjs`, which checks both manifest fields explicitly. The marco-sdk and xpath versions were unified to match the extension version starting at v2.5.0., synchronized exactly across `chrome-extension/manifest.json` (`version` and `version_name`), `src/shared/constants.ts`, `src/options/sections/AboutSection.tsx`, `standalone-scripts/macro-controller/src/instruction.ts`, `standalone-scripts/marco-sdk/src/instruction.ts`, and `standalone-scripts/xpath/src/instruction.ts`. Version consistency is validated via `scripts/check-version-sync.mjs`, which checks both manifest fields explicitly. The marco-sdk and xpath versions were unified to match the extension version starting at v2.5.0.
